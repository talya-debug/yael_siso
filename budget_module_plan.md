# מודול ניהול תקציב לקוח — תכנון

## אישור יעל
- תקציב: 1,500 ש"ח
- הכל מרוכז במערכת
- **אין פורטל לקוח** — הלקוח לא רואה לינק ישיר לחשבוניות
- התהליך יותר ידני — הבנות שולטות במה שהלקוח מקבל

---

## הרעיון

ניהול תקציב פרויקט מלא בתוך המערכת. כל פרויקט מקבל טבלת תקציב עם שורה לכל ספק/תחום. הבנות מנהלות תשלומים, מעלות חשבוניות, ושולחות בקשות תשלום ללקוח — הכל מתוך המערכת, בלי שהלקוח נכנס למערכת בעצמו.

---

## מה נבנה

### 1. טבלת תקציב בתוך פרויקט
- טאב חדש "Budget" בדף פרויקט (ליד Tasks, Client Card)
- שורה לכל ספק/תחום:
  - נושא (למשל: "ארונות מטבח")
  - ספק (בחירה מרשימת ספקים קיימת)
  - מחיר מתוכנן
  - מע"מ (חישוב אוטומטי 17%)
  - סה"כ כולל מע"מ
  - תנאי תשלום (טקסט חופשי)
  - הערות
  - סטטוס: ממתין / שולמה מקדמה / שולם במלואו

### 2. לוג תשלומים
- בכל שורה, כפתור "רשום תשלום"
- תאריך + סכום + הערה
- סטטוס מתעדכן אוטומטית לפי סכום ששולם מול מחיר מתוכנן

### 3. סיכום תקציב
- בראש הטבלה:
  - סה"כ תקציב מתוכנן
  - סה"כ שולם
  - יתרה
  - אחוז ביצוע (בר התקדמות)

### 4. בקשת תשלום במייל
- כפתור "שלח בקשת תשלום" על שורה
- נפתח מודאל תצוגה מקדימה (כמו בחתימות) — ניתן לערוך
- נשלח מייל מ-hello@yaelsiso.com ללקוח עם:
  - סכום לתשלום
  - שם ספק
  - פרטי בנק של הספק (אם קיימים)
  - אפשרות לצרף חשבונית
- נרשם בלוג: "בקשת תשלום נשלחה ללקוח"

### 5. העלאת חשבוניות
- העלאת PDF/תמונה לכל שורה בתקציב
- כל חשבונית מקבלת לינק ציבורי (כמו יומן העבודה)
- קלואי שולחת ללקוח את הלינקים במייל כשצריך
- חשבוניות נשמרות על השורה ואפשר לצפות/להוריד

### 6. פרטי בנק על ספקים
- הוספת שדות לכרטיס ספק: שם חשבון, בנק, סניף, מספר חשבון
- פעם אחת — משתמשים בכל הפרויקטים
- נשלף אוטומטית במייל בקשת תשלום

---

## מה לא נבנה (יקר מדי — 3,000 ש"ח)
- ~~פורטל לקוח עצמאי~~ — הלקוח לא נכנס למערכת בעצמו

## איך הלקוח מקבל מידע
- קלואי שולחת מייל חודשי מהמערכת עם סיכום תקציב
- חשבוניות נשמרות במערכת עם לינק ציבורי — קלואי מצרפת למייל
- הכל ידני ובשליטת קלואי — אין גישה ישירה ללקוח

---

## ארכיטקטורה

### DB — טבלאות חדשות

**budget_items**
- id (uuid, PK)
- project_id (FK → projects)
- supplier_id (FK → suppliers, nullable)
- category (text) — "ארונות מטבח", "אינסטלציה" וכו
- planned_amount (numeric)
- vat_rate (numeric, default 17)
- total_with_vat (numeric, computed)
- payment_terms (text)
- notes (text)
- status (text: pending / partial / paid)
- sort_order (int)
- created_at (timestamp)

**budget_payments**
- id (uuid, PK)
- budget_item_id (FK → budget_items)
- amount (numeric)
- payment_date (date)
- note (text)
- created_at (timestamp)

**budget_invoices**
- id (uuid, PK)
- budget_item_id (FK → budget_items)
- file_name (text)
- file_data (text) — base64 כמו בחתימות
- uploaded_at (timestamp)

**suppliers** — שדות חדשים
- bank_name (text)
- bank_branch (text)
- bank_account (text)
- account_holder (text)

### UI — קומפוננטות

**BudgetTab** (בתוך ProjectDetail)
- סיכום תקציב למעלה (מתוכנן/שולם/יתרה/אחוז)
- טבלת שורות (budget_items)
- כפתורים: הוסף שורה, שלח בקשת תשלום, רשום תשלום, העלה חשבונית
- מודאל תצוגת מייל (reuse מהחתימות)

### API
- /api/send-email — כבר קיים, ישמש גם לבקשות תשלום

---

## סדר בנייה

### שלב 1: DB
1. יצירת טבלאות budget_items, budget_payments, budget_invoices
2. הוספת שדות בנק לטבלת suppliers

### שלב 2: Backend
1. RLS policies לטבלאות חדשות
2. וידוא ש-send-email עובד (כבר עובד)

### שלב 3: UI
1. טאב Budget בדף פרויקט
2. טבלת תקציב + הוספת שורות
3. סיכום תקציב (מתוכנן/שולם/יתרה)
4. מודאל רישום תשלום
5. העלאת חשבוניות
6. מודאל שליחת בקשת תשלום במייל
7. שדות בנק בכרטיס ספק

### שלב 4: ייבוא
1. ייבוא מאקסל BITTON BUDGET כדוגמה ראשונה
